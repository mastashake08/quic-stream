{
    "name": "QUIC WebTransport MultiStream Server",
    "dockerFile": "Dockerfile",
    "context": "..",
    "postCreateCommand": "pip install -r requirements.txt",
    "remoteUser": "root",
    "forwardPorts": [4433, 8080]
}
